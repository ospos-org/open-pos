import { useEffect } from "react"

export default function PromotionList({
    variant_id
}: {
    variant_id: string | undefined
}) {

    useEffect(() => {
    }, []);

    return (
        <div>

        </div>
    )
}